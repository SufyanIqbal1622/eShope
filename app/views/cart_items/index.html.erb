<h1> Cart Items </h1>

<% @cart_items.each do |cart_item| %>
  <%= cart_item.inspect %>
  <p> <span> <%= cart_item.user_id %> </span> <span> <%= cart_item.product_id %> </span> <span> <%= cart_item.quantity %> </span> </p>

<% end %>
